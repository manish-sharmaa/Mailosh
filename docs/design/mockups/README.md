# Approved Phase 1 mockups (2026-09-02)

HTML fragments shown in the brainstorming companion; open them in a browser (they load Inter from Google Fonts for preview only — the product self-hosts it).

- `layout.html` — the frame (approach C). Decision: **list-first**, reading pane optional (B).
- `visual-style.html` — three palettes light + dark. Decision: **C · Graphite & Blue**.
- `key-moments.html` — conversation view, compose dock, ⌘K palette (dark), densities. Decision: **Comfortable** default density.

Token values live in the spec (`docs/specs/2026-09-02-phase1-webmail-design.md` §4). These files are the visual reference for browser QA in every Phase 1 plan.
